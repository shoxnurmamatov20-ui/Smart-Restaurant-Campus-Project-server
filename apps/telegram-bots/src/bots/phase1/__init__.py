"""Phase-1 bot handler modules — the 10 bots a restaurant needs from day one.

Files here are auto-discovered by `bot_manager._load_router` via
`src.bots.phase1.<bot_key>` lookup.

Active Phase-1 keys: guest, waiter, kitchen, courier, manager, owner, loyalty,
reservation, feedback, supplier.

`guest.py` and `waiter.py` currently live at `src/bots/` (flat); they will move
here as the catalog grows.
"""
