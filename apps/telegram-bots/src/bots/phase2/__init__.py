"""Phase-2 bot handler modules — 40 specialised bots.

Files here (when created) are auto-discovered by `bot_manager._load_router`
via `src.bots.phase2.<bot_key>` lookup.

Groups: operatsion (stock_alert, waste, haccp, shift_swap, payroll, training,
recruiting, equipment, energy, security), marketing (birthday, winback,
catering, corporate, gift_card, review_watch, menu_ai, nutrition, allergen,
queue), yetkazib berish (aggregator, delivery_zone, driver_dispatch, tracking)
and moliya (cash_alert, fiscal, debt, budget).

Per-branch (br_*) and per-concept bots live in src/bots/branch and
src/bots/concept.
"""
