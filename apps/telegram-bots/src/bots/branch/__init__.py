"""Per-branch bot handler modules — one internal channel per venue.

Files here are auto-discovered via `src.bots.branch.<bot_key>` lookup.
Keys start with `br_` (e.g. br_chilonzor, br_yunusobod).
"""
