"""Shared handler builders reusable across multiple bots.

- `onboarding.py` — /start + phone verify (existing, reused)
- `common.py` — /help, /cancel, /lang (universal commands)
- `errors.py` — global exception handler
"""
