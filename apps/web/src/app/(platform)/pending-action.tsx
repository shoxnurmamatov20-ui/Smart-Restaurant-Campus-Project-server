/**
 * Moved to `src/components/pending-action.tsx` once the console needed it too.
 * Re-exported rather than duplicated: two copies of "say what is not wired yet"
 * is how the two consoles end up saying it differently.
 */
export { PendingAction } from '@/components/pending-action';
